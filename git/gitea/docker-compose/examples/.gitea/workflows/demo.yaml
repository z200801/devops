name: demo

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Print environment
        run: |
          echo "Runner: $RUNNER_NAME"
          echo "OS: $(uname -a)"

      - name: Run tests
        run: echo "Replace this step with your actual test command"
